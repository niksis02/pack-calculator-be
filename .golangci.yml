version: "2"

run:
  timeout: 5m
  go: "1.26"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - bodyclose

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
